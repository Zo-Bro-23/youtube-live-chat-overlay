const getAuthUrl = require('zoauth/getAuthUrls')
const getToken = require('zoauth/getToken')
const getDetails = require('zoauth/getDetails')
const {
    setCredentials
} = require('zoauth/setCredentials')
const amazon = require('zoauth/amazon')
const discord = require('zoauth/discord')
const facebook = require('zoauth/facebook')
const github = require('zoauth/github')
const google = require('zoauth/google')
const microsoft = require('zoauth/microsoft')
const {
    runAuthServer
} = require('zoauth/runAuthServer')
const {
    expressApp
} = require('zoauth/runAuthServer')
const getApi = require('zoauth/getApi')
const postApi = require('zoauth/postApi')
const demoApp = require('zoauth/demoApp')

module.exports = {
    getAuthUrl,
    getToken,
    getDetails,
    setCredentials,
    amazon,
    discord,
    facebook,
    github,
    google,
    microsoft,
    runAuthServer,
    getApi,
    postApi,
    demoApp,
    expressApp
}